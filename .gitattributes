# apus-qwen is byte-exact everywhere: golden fixtures, conformance pairs,
# and tokenizer data are compared bitwise by the C tests. Never let git
# translate line endings (Windows autocrlf CRLF'd the m2 conformance
# pairs on checkout in the Apus M15 port — same hazard here).
* -text
